//front end mainfunction main(prm) {	var qGET = new URLSearchParams(location.search);	if(qGET.get("requisitionId") == "new") {		document.getElementById("rqList").style.display = "none";		document.getElementById("rq").style.display = "block";		document.getElementById("rqId").innerText = "new";	} else {		var url = "backend.php";		if(qGET.has("requisitionId")) url = "backend.php?requisitionId=" + qGET.get("requisitionId");				if(prm == "button") {			url += "&save=" + save();		}		var xmlhttp = new XMLHttpRequest();		xmlhttp.onreadystatechange = function() {			if (this.readyState == 4 && this.status == 200) {							console.log(this.responseText);				obj = JSON.parse(this.responseText);				type = Object.keys(obj)[0]; //either 'rq' or 'rqlist'				if(type == "rqlist") {					rq_list(obj[type]); //does the rq list				} else {					rq_form(obj[type]); //does the rq form				}			}		}				console.log(url);		xmlhttp.open("GET", url, true);		xmlhttp.send();	}}